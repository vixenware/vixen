// Rung 015 — ||, &&, ! as ordinary boolean operators.
#[test]
fn boolean_operators() -> Stream<Check> {
    yield expect(true || false);
    yield expect(!(false && true));
    yield expect_eq(!true, false);
    yield expect((1 < 2) || (2 < 1));
}
