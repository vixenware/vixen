//! reject: unknown method pop
//! at: 8

// Rung 032 — pop does not exist; mutation-shaped names have no referent.
#[test]
fn no_pop() -> Stream<Check> {
    let xs = [1, 2, 3];
    yield expect_eq(xs.pop(), (3, [1, 2]));
}
