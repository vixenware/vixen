// Rung 042 — with deliberately describes a map with one rebound key.
#[test]
fn map_overwrite() -> Stream<Check> {
    let m = %{"k" => 1};
    let m2 = m.with ("k", 2);
    yield expect_eq(m.get("k"), 1);
    yield expect_eq(m2.get("k"), 2);
    yield expect_eq(m2.len(), 1);
}
