// Second phase for rung 103: compute renamed; downstream location churns.
fn compute_thrice(n: Int) -> Int { n * 3 }
fn render(n: Int) -> String { "v" + n.to_string() }

#[test]
fn rename_is_cold() -> Stream<Check> {
    yield expect_eq(render(compute_thrice(4)), "v12");
}
