//! reject: too many arguments
//! at: 5

// Rung 112 — arity errors point at the call, not the definition.
fn one(a: Int) -> Int { a }
#[test]
fn t() -> Stream<Check> {
    yield expect_eq(one(1) where { extra: 2 }, 1);
}
