// Rung 019 — tuple destructuring in let.
fn min_max(a: Int) where { b: Int } -> (Int, Int) {
    if a < b { (a, b) } else { (b, a) }
}

#[test]
fn let_destructuring() -> Stream<Check> {
    let (lo, hi) = min_max(9) where { b: 4 };
    yield expect_eq(lo, 4);
    yield expect_eq(hi, 9);
}
