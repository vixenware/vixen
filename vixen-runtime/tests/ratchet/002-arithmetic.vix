// Rung 002 — integer literals and arithmetic; expect_eq on Int.
#[test]
fn arithmetic() -> Stream<Check> {
    yield expect_eq(2 + 2, 4);
    yield expect_eq(10 - 3 * 2, 4);
    yield expect_ne(1, 2);
}
