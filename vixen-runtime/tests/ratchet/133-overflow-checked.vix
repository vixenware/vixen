// Rung 133 — Int is 64-bit and overflow is a typed failure, not a wrap.
#[test]
fn overflow_checked() -> Stream<Check> {
    let big = 9223372036854775807;      // i64::MAX
    yield expect_eq(big - 1 + 1, big);
    let boom = big + 1;
    yield failed_with(boom, "overflow");
}
