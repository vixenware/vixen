// Rung 011 — < <= > >= derive from <=>, everywhere.
#[test]
fn derived_comparisons() -> Stream<Check> {
    yield expect(1 < 2);
    yield expect("apple" <= "banana");
    yield expect((1, "b") > (1, "a"));
}
