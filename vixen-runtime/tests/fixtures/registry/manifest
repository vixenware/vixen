# Offline fixture registry manifest: one artifact per line.
# <name> <url> <vix blake3 ContentHash of the archive Blob, hex>
# The ContentHash is the framed Blob value identity (machine.identity.blake3,
# one hash definition) computed at lock time; the URL is a provenance
# coordinate resolved against the fixture store, never the network.
#
# Two shapes of the same archive on purpose: the plain one is uncompressed ustar
# (what `untar` parses directly), the `-gzipped` one is what a registry actually
# serves for a `.crate` — gzipped tar, read as `untar(blob.gunzip())`. The plain
# one alone hid for a long time that nothing here could unpack a real artifact.
tokio-1.52.3.crate fixture://registry/tokio-1.52.3.crate 01a321713d1aee2ff294bd98772239c0855084fb09a6184c10adfc5c6a8c5596
tokio-1.52.3-gzipped.crate fixture://registry/tokio-1.52.3-gzipped.crate 5569ec7b081795a3f8605322a481733104e545636b8b483e33d769cb4320f3f8
