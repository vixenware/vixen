[package]
name = "vix-wire"
version = "0.1.0"
edition = "2024"
description = "The `vix::` facet extension-attribute grammar (wire-type mapping)"
publish = false

[dependencies]
facet = { path = "../facet" }
