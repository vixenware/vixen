// Rung 131 — unary minus exists; negative literals behave.
#[test]
fn negative_numbers() -> Stream<Check> {
    let n = -5;
    yield expect_eq(n + 10, 5);
    yield expect_eq(-n, 5);
    yield expect(-1 < 0);
    yield expect_eq((-2) * (-3), 6);
}
