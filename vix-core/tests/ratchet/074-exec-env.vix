// Rung 074 — environment variables are declared values, not ambient leaks.
#[test]
fn exec_env(sh: Sh) -> Stream<Check> {
    let out = exec sh`-c "echo $GREETING"` where { env: %{ "GREETING" => "hi" } };
    yield expect_eq(out.stdout.trim(), "hi");
}
