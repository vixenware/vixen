// Rung 006 — record declaration, literals, field access. (values.md)
struct Point { x: Int, y: Int }

#[test]
fn records() -> Stream<Check> {
    let p = Point { x: 3, y: 4 };
    yield expect_eq(p.x, 3);
    yield expect_eq(p.y, 4);
}
