// Rung 021 — destructuring in closure parameters.
#[test]
fn closure_destructuring() -> Stream<Check> {
    let apply = |args: ((Int, Int), fn((Int, Int)) -> Int)| {
        let ((a, b), f) = args;
        f((a, b))
    };
    yield expect_eq(apply(((3, 4), |(x, y)| x + y)), 7);
}
