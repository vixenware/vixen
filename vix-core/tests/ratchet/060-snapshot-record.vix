// Rung 060 — snapshots: any value renders structurally, no Debug impls.
struct Dep { name: String, req: String, optional: Bool }

#[test]
fn snapshot_record() -> Stream<Check> {
    let d = Dep { name: "mio", req: "^0.8", optional: false };
    yield expect_snapshot(d, "dep-mio");
}
