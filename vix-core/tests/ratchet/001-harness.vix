// Rung 001 — the harness itself: a test declaration is a demandable check.
// Certifies: testing.md `test NAME { }`, expect().
#[test]
fn the_ratchet_begins() -> Stream<Check> {
    yield expect(true);
}
