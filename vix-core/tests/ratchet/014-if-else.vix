// Rung 014 — if/else is an expression.
fn sign(n: Int) -> Int {
    if n > 0 { 1 } else if n < 0 { -1 } else { 0 }
}

#[test]
fn if_else() -> Stream<Check> {
    yield expect_eq(sign(42), 1);
    yield expect_eq(sign(-5), -1);
    yield expect_eq(sign(0), 0);
}
